# Variables de producción para Heroku
# NO commitear este archivo - usar heroku config:set

# Estas variables se configuran en Heroku Dashboard o CLI:
# heroku config:set VARIABLE=valor

# Base de datos (Aiven Production)
DB_PRIMARY_HOST=mysql-prod.aivencloud.com
DB_PRIMARY_USER=produser
DB_PRIMARY_PASSWORD=xxxx
DB_PRIMARY_NAME=bsi_production
DB_PRIMARY_PORT=23108
DB_PRIMARY_SSL_CA=./src/DB/crt/ca.pem

# JWT (Generar nuevos secrets para producción!)
JWT_SECRET=generar-secret-seguro-de-64-caracteres
JWT_REFRESH_SECRET=otro-secret-seguro-de-64-caracteres

# CORS - Cambiar a tu dominio de Netlify
CORS_ORIGIN=https://tu-app.netlify.app

# Node
NODE_ENV=production

# El puerto lo asigna Heroku automáticamente
# PORT=$PORT

# Desactivar debug en producción
LOG_LEVEL=info
ENABLE_SWAGGER=false