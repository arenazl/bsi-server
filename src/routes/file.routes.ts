import { Router } from 'express';

const router = Router();

// Placeholder - implementar después
router.get('/', (req, res) => {
  res.json({ message: 'Files endpoint - TODO' });
});

export default router;