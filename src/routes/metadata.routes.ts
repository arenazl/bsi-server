import { Router } from 'express';

const router = Router();

// Placeholder - implementar después
router.get('/', (req, res) => {
  res.json({ message: 'Metadata endpoint - TODO' });
});

export default router;